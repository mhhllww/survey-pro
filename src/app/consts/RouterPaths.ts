export enum RouterPaths {
  HOME = '/',
  ANALYTICS = '/analytics',
  LOGIN = '/login',
  REGISTER = '/register',
  CREATE = '/dashboard/survey/create/:surveyId',
  DASHBOARD = '/dashboard',
  SURVEY = '/survey/:surveyId',
}
